#ifndef CONFIG_HPP
#define CONFIG_HPP

#include <map>
#include <string>

typedef map<string, string>                  Config;
typedef map<string, string>::iterator        ConfigIt;
typedef map<string, string>::const_iterator  ConfigConstIt;

#endif

