TODO




8. Поддрежка обновлений, нужно чтобы web_InI проверяла наличие обновлений
   и если на сервере есть новая версия, предлагал пользователю перезапуститься.
   -- сделал, теперь периодически проверяется версия InI на сервере и если она более новая,
      то пользователю предлагают перезапустить клиент. Исправлено в версии 0.10

*7. Время сообщений проставляется не из сообщения, а во время получения сообщения от движка
    т.е. если перезайти, то время всех сообщений слетает :)
    -- исправлено в 0.21 (rev 168)

6. Если xmlhttp отваливает по timeout, он должен сам, автоматически реконнектиться к ipnoise
    -- сделал, теперь переконнективается, исправлено в версии 0.10
-------------------
2009-07-24 14:57:05.784 ERROR ? 4 Cannot read XML data from '/API/cmd/'
Detail: xmlhttp.status: 0
Stack:
plog("ERROR","Cannot read XML data from '/API/cmd/'","xmlhttp.status: 0",4)@http://ipnoise.chechnev.ru:2210/web_InI/log.js:80
perror("Cannot read XML data from '/API/cmd/'","xmlhttp.status: 0",4)@http://ipnoise.chechnev.ru:2210/web_InI/log.js:142
-------------------

5. Нужно сделать уведомления о приходе новых сообщений
    -- сделал, добавил сласс Sound он через flash умеет играть mp3, заимплеменчено в версии 0.9

4. Окошко с историей приходиться прокручивать,
    т.к. когда приходят новые сообщения, оно не скролится в конец и не видно что пришло
    -- добавил прокрутку на последнее сообщение

3. После отправки сообщения теряется фокус и приходится заново кликать на поле для ввода мессаги.
    -- проблемма таже самая что и в баге #1, исправлено в версии 0.8

*2. Когда кликаем по ссылки, открывает окошко с просмотром, но если картинка больше чем окошко,
    то вся она не влазиет, а заресайзить,развернуть нет возможности.

1. Сделать так, чтобы поле для ввода текста не стиралось, когда приходят новые сообщения.
   -- исправлено в 0.8 теперь, проблемма была в том, что в объект окна вставлялся innerHtml,
      а не элемент, а т.к. innerHtml вставлял и хистори и поле для ввода текста, то все перерисовывалось
      добавил ф-ю self.setBodyElement(element) для установки именно элемента в качестве контента окна


